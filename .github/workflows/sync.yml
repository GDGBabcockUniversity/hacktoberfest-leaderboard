name: Sync leaderboard
on:
  schedule: [{ cron: '*/10 * * * *' }]
  workflow_dispatch:
jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - run: curl --fail --request POST -H "x-sync-secret: ${{ secrets.SYNC_SECRET }}" "${{ secrets.LEADERBOARD_URL }}/api/sync"
